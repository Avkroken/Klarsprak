{
  "name": "klarsprak",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler d1 migrations apply klarsprak-db --remote && wrangler deploy && node scripts/check-production-domain.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
